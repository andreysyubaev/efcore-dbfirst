<Page x:Class="efcore_dbfirst.Pages.EditTagPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:efcore_dbfirst.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditTagPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Width="240">
            <StackPanel>
                <Label Content="Название тега"/>
                <TextBox Text="{Binding Name}" materialDesign:HintAssist.Hint="Введите название тега"/>
            </StackPanel>
            <Button Click="Edit" Content="Изменить" Margin="0,8"/>
            <Button Click="Delete" Content="Удалить" Margin="0,8"/>
        </StackPanel>
        <Button Click="Back" Content="Назад" Grid.Row="1"/>
    </Grid>
</Page>
