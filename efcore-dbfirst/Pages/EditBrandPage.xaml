<Page x:Class="efcore_dbfirst.Pages.EditBrandPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:efcore_dbfirst.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:validator="clr-namespace:efcore_dbfirst.Validators"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Изменение бренда">
    <Page.Resources>
        <validator:BrandCategoryTagValidator x:Key="brandCategoryTagValidator"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Width="320">
            <Label Content="Изменение бренда"
                   HorizontalContentAlignment="Center"
                   FontSize="24" FontWeight="SemiBold"
                   Margin="16"/>
            <StackPanel>
                <Label Content="Название бренда"/>
                <TextBox x:Name="Name" Validation.ErrorTemplate="{x:Null}" materialDesign:HintAssist.Hint="Введите название бренда">
                    <TextBox.Text>
                        <Binding Path="Name"
                                 UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validator:BrandCategoryTagValidator/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Name, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <Button Click="Edit" Content="Изменить" Margin="0,8"/>
            <Button Click="Delete" Content="Удалить" Margin="0,8"/>
        </StackPanel>
        <Button Click="Back" Content="Назад" Grid.Row="1"/>
    </Grid>
</Page>
