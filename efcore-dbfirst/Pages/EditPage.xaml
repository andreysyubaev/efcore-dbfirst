<Page x:Class="efcore_dbfirst.Pages.EditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:efcore_dbfirst.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Изменение товара" Loaded="LoadList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        HorizontalAlignment="Center" VerticalAlignment="Center" 
                        Width="320">
                <Label Content="Изменение товара"
                       HorizontalContentAlignment="Center"
                       FontSize="24" FontWeight="SemiBold"
                       Margin="16"/>
                <StackPanel>
                    <Label Content="Название"/>
                    <TextBox Text="{Binding Product.Name}" materialDesign:HintAssist.Hint="Введите название"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Описание"/>
                    <TextBox Text="{Binding Product.Description}" materialDesign:HintAssist.Hint="Введите описание"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Цена"/>
                    <TextBox Text="{Binding Product.Price}" materialDesign:HintAssist.Hint="Введите цену"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Количество"/>
                    <TextBox Text="{Binding Product.Stock}" materialDesign:HintAssist.Hint="Введите количество"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Рейтинг"/>
                    <TextBox Text="{Binding Product.Rating}" materialDesign:HintAssist.Hint="Введите рейтинг"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Дата создания"/>
                    <DatePicker SelectedDate="{Binding Product.CreatedAt}"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Категория"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Product.CategoryId}" />
                </StackPanel>
                <StackPanel>
                    <Label Content="Бренд"/>
                    <ComboBox ItemsSource="{Binding Brands}"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Product.BrandId}" />
                </StackPanel>
                <Button Click="Edit" Content="Изменить" Margin="0,8"/>
                <Button Click="Delete" Content="Удалить" Margin="0,8"/>
            </StackPanel>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <ListView Grid.Row="0"
                          ItemsSource="{Binding Tags}" 
                          SelectedItem="{Binding SelectedTag}"
                          Name="TagsList"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="ID: "/>
                                <Run Text="{Binding Id}"/>
                                <Run Text=" | "/>
                                <Run Text="Название: "/>
                                <Run Text="{Binding Name}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Click="AddTag" Content="Добавить"/>
                    <Button Grid.Column="1" Click="RemoveTag" Content="Убрать"/>
                </Grid>
                <ListView Grid.Row="2"
                          ItemsSource="{Binding ProductTags}"
                          SelectedItem="{Binding SelectedProductTag}"
                          x:Name="ProductTagsList"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="ID: "/>
                                <Run Text="{Binding Id}"/>
                                <Run Text=" | "/>
                                <Run Text="Название: "/>
                                <Run Text="{Binding Name}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
        
        <Button Click="Back" Content="Назад" Grid.Row="1"/>
    </Grid>
</Page>
